---
import { reviewSchedule } from '../data/meta';
---

<section id="review">
  <div class="section-head">
    <div class="kicker">保持不忘</div>
    <h2>复习制度</h2>
    <p>
      每天 15 分钟复盘里，前 8 分钟固定用来做间隔重复。不额外占时间，但决定了第 8 周你还剩下多少。
      前三行不用自己记：天页的「今日复盘」按学习日往回数 1 / 3 / 7 天，把该复习的直接摆出来。
    </p>
  </div>

  <div class="card">
    <div class="tbl-wrap">
      <table>
        <thead>
          <tr><th>时间点</th><th>做什么</th><th>用时</th></tr>
        </thead>
        <tbody>
          {
            reviewSchedule.map((r) => (
              <tr>
                <td>{r.when}</td>
                <td>{r.what}</td>
                <td>{r.cost}</td>
              </tr>
            ))
          }
        </tbody>
      </table>
    </div>
  </div>
</section>
