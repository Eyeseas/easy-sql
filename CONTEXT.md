# easy-sql（八周 SQL 冲刺计划站点）

剧情化 PostgreSQL 面试备考站点：56 个学习日随业务剧情展开，每个学习日是一次「学练盘」三段学习会话。

## Language

**学习日 (Day)**:
课程的最小单位（D01–D56），带剧情、学/练/盘三段时间盒与过关标准。页面上的「天」都指它。
_Avoid_: 课程日、天数

**计时会话 (Timer Session)**:
某一个学习日的「学练盘」三段连续计时进程。全局至多同时存在一个；开启另一天会终结当前会话。
_Avoid_: 多开计时、并行计时、后台任务

**呈现位 (Presentation Slot)**:
进行中的计时会话被渲染出来的位置，全站共三个：周页卡片内、天页右栏、兜底横条。前两个只在其所属学习日正在计时且用户正停留在该页面时可见。
_Avoid_: 计时器组件、底栏（指非兜底场景时）

**兜底横条 (Fallback Bar)**:
全局底部横条（含三段进度、剩余时间与控制键），三个呈现位中的兜底那个。仅当进行中的计时会话不属于当前页面时出现，平时隐藏；不含完课提示。
_Avoid_: 计时器（指组件时）、底部播放器

**完课提示 (Finish Prompt)**:
三段全部跑完后出现的全局一次性提醒（「标记完成 / 还没」），由学习者自己决定这一天算不算过关。独立于兑底横条，不随呈现位内联。
_Avoid_: 完成弹窗、结算条
